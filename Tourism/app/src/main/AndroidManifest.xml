<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tamoghna.tourism">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SunderbanActivity" />
        <activity android:name=".VictoriaActivity" />
        <activity android:name=".EdenActivity" />
        <activity android:name=".KolkataActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".APActivity" />
        <activity android:name=".TirupatiActivity" />
        <activity android:name=".VijayawadaActivity" />
        <activity android:name=".VishakapatnamActivity"></activity>
    </application>

</manifest>